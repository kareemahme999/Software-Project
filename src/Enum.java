 enum NotificationType {
    BUDGET_NEAR_LIMIT,
    BUDGET_EXCEEDED,
    GOAL_COMPLETED
}
 enum BudgetStatus {
    ON_TRACK,
    NEAR_LIMIT,
    EXCEEDED
}
 enum GoalStatus {
    IN_PROGRESS,
    NEAR_LIMIT,
    COMPLETED,
    CANCELLED
}
